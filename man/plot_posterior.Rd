% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stan_plot.R
\name{plot_posterior}
\alias{plot_posterior}
\title{Plots the marginal posteriors of each parameter in a stanfit object}
\usage{
plot_posterior(
  posterior_samples,
  prior_samples = NA,
  true_param_values = NA,
  params_desired = NA,
  transforms = NA,
  labels = NA,
  skip_stanfit_to_dt = NA
)
}
\arguments{
\item{posterior_samples}{A stanfit object e.g. as returned by
\code{rstan::sampling()}. This should contain samples from the posterior.}

\item{prior_samples}{A stanfit object containing samples from the prior, so
that the prior and posterior distributions are overlaid. The default value
of NA means only the posterior is plotted.}

\item{true_param_values}{A named numeric vector: the names are those of
parameters, and the values are the true values (known from simulation
truth). Using this argument means the output plot will include vertical
lines at the true values. The default value of NA means no such lines are
included.}

\item{params_desired}{A character vector of parameter names. Using this
argument means only named parameters will be included in the plot. The
default value of NA means all parameters are included.}

\item{transforms}{A named list of functions, with the names being parameter
names. Using this argument means each parameter named in this list will be
transformed by applying the associated function to its sampled values
before plotting. e.g. specifying \code{list(x=log)} would result in the \code{log}
function being applied to all values of parameter \code{"x"}. Note that the
names of parameters as they appear in the plot are not updated to reflect
the functions applied, therefore this argument should usually be used
together with the \code{labels} argument. The default value of NA means no
transforms are applied to any parameter.}

\item{labels}{A named character vector, with the names being the names of
parameters as they are named within the stanfit object, and the values
being new names to use instead. e.g. if \code{transforms=list(x=log)} were
specified, \verb{labels(x="log(x)"} would make sense, so that the label of the
plot of the posterior for x reflects the log transformation.}

\item{skip_stanfit_to_dt}{If this argument is set to \code{TRUE}, the
\code{posterior_samples} argument (and the \code{prior_samples} argument if used)
should be used to provide a datatable of samples instead of a stanfit
object of samples, for example after having already run
\code{\link[=stanfit_to_dt]{stanfit_to_dt()}} on the stanfit objects. This allows e.g. manual
renaming of parameters before plotting.}
}
\value{
A ggplot object.
}
\description{
Plots the marginal posteriors of each parameter in a stanfit object
}
\examples{
eg <- mastiff::stan_example_regression
plot_posterior(eg$posterior_samples)
plot_posterior(eg$posterior_samples,
               prior_samples = eg$prior_samples,
               true_param_values = eg$true_values)
}
