% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stan_summaries.R
\name{posterior_means}
\alias{posterior_means}
\alias{posterior_medians}
\title{Calculates the mean or median for each parameter for each stanfit object}
\usage{
posterior_means(stanfit_list, params_desired = NA)

posterior_medians(stanfit_list, params_desired = NA)
}
\arguments{
\item{stanfit_list}{A list of stanfit objects e.g. as returned by
\code{\link[rstan:stanmodel-method-sampling]{rstan::sampling()}}. The function's name reflects the assumption that these
contain samples from the posterior, but they could contain samples from the
prior.}

\item{params_desired}{A character vector with the names of those parameters
in the stanfit object that should be included in the output. The default
value of \code{NA} means all parameters are included except \code{"lp__"} (which is not
actually a parameter: Stan uses it to record the value of the target
probability density).}
}
\value{
A 2D array: the first index for the parameter, the second index for
the stanfit object.
}
\description{
In presto the typical list supplied to this would have one stanfit object
for each simulated trial.
}
\examples{
   eg <- mastiff::stan_example_regression
   posterior_means(list(eg$posterior_samples, eg$prior_samples))
   posterior_medians(list(eg$posterior_samples, eg$prior_samples))
}
