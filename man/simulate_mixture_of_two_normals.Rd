% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mixture_of_two_normals.R
\name{simulate_mixture_of_two_normals}
\alias{simulate_mixture_of_two_normals}
\title{Simulate (randomly draw) numbers from a mixture of two normal distributions.}
\usage{
simulate_mixture_of_two_normals(
  n = 500,
  groups = LETTERS[1:5],
  group_frequencies = rep(1/length(groups), length(groups)),
  mu_0 = 0,
  mu_1 = mu_0 + 3,
  sd_0 = 0.5,
  sd_1 = 1,
  p = 0.5,
  sd_groups = 1
)
}
\arguments{
\item{n}{A non-negative integer: the number of observations to simulate.}

\item{groups}{A character vector: a set of categories that differ in
their proportions for the mixture. Specify \code{character(0)} to have all
observations in the same category, i.e. all observations have the same
probability of coming from one component of the mixture or the other.}

\item{group_frequencies}{A numeric vector of the same length as
\code{groups}, each value being the frequency of that group (as a
proportion between 0 and 1).}

\item{mu_0}{A number: the smaller of the means of the two normals.}

\item{mu_1}{A number: the larger of the means of the two normals.}

\item{sd_0}{A non-negative number: the standard deviation of the normal with
mean mu_0.}

\item{sd_1}{A non-negative number: the standard deviation of the normal with
mean mu_1.}

\item{p}{The probability of an observation coming from the normal with mean
mu_1.}

\item{sd_groups}{A non-negative number: the standard deviation of the
normal variability between regression coefficients for the groups (on a
logit scale).}
}
\value{
A dataframe with \code{n} rows, one per observation. The column \code{y}
contains the values drawn from the normal mixture. The column \code{d} contains
a logical variable for whether that observation came from the normal
with mean \code{mu_1}.
}
\description{
Simulate (randomly draw) numbers from a mixture of two normal distributions.
}
\examples{
df <- simulate_mixture_of_two_normals()$data
hist(df$y)
hist(df[df$d, ]$y)   # the normal with mean mu_1
hist(df[! df$d, ]$y) # the normal with mean mu_0
}
