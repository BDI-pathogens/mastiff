% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{uniroot.vectorized}
\alias{uniroot.vectorized}
\title{Vectorised uniroot}
\usage{
uniroot.vectorized(f, lower, upper, tol = 1e-08, itmax = 100, eps = 1e-10)
}
\arguments{
\item{f}{the objective function to optimise over. Must take a single vector
as input and output a vector of values}

\item{lower}{vector of left hand end points of the intervals to optimise over}

\item{upper}{vector of right hand end points of the intervals to optimise over}

\item{tol}{the desired accuracy (convergence tolerance)}

\item{itmax}{the maximum number of iterations}

\item{eps}{XXXXX TODO}
}
\value{
vector of roots for each interval \code{[lower, upper ]}
}
\description{
A vectorised version of the Brent root algorithm. Useful for solving many
similar optimisation problems where evaluation of the objective function can
be calculated more efficiently when vectorised
}
\examples{
f <- function( x ) ( x^2 - 1 ) * ( x^2 - 2 )
lower <- c( -2,  -1.2,   0, 1.2 )
upper <- c( -1.2,   0, 1.2,   2 )
uniroot.vectorized( f, lower, upper )

}
