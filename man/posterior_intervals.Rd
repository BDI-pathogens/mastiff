% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stan_summaries.R
\name{posterior_intervals}
\alias{posterior_intervals}
\title{Calculates central probability intervals for each parameter for each stanfit
object}
\usage{
posterior_intervals(stanfit_list, prob, params_desired = NA, ...)
}
\arguments{
\item{stanfit_list}{A list of stanfit objects e.g. as returned by
\code{rstan::sampling()}. The function's name reflects the assumption that these
contain samples from the posterior, but they could contain samples from the
prior.}

\item{prob}{A single number between 0 and 1 specifying the amount of
probability to be contained in the interval. e.g. Specifying 0.95 results
in an interval that spans the 2.5th to 97.5th percentiles of the
distribution.}

\item{params_desired}{A character vector with the names of those parameters
in the stanfit object that should be included in the output. The default
value of \code{NA} means all parameters are included except \code{"lp__"} (which is
not actually a parameter: Stan uses it to record the value of the target
probability density).}

\item{...}{Additional arguments to pass to
\code{rstantools::posterior_interval()}.}
}
\value{
A 3D array: the first index for the parameter, the second for the
percentile, the third for the stanfit object.
}
\description{
The function is a simple wrapper around \code{rstantools::posterior_interval()}.
In presto the typical list supplied to this would have one stanfit object
for each simulated trial.
}
\examples{
   eg <- mastiff::stan_example_regression
   posterior_intervals(list(eg$posterior_samples, eg$prior_samples), prob =
   0.95)
}
