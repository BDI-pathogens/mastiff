% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/check_types.R
\name{check_numeric}
\alias{check_numeric}
\title{Checks a variable is a single number (and optionally, in a range and not NA)}
\usage{
check_numeric(
  x,
  lower = -Inf,
  lower_inclusive = TRUE,
  upper = Inf,
  upper_inclusive = TRUE,
  allow_missing = FALSE
)
}
\arguments{
\item{x}{The variable to check.}

\item{lower}{The lower limit for \code{x} (default = \code{-Inf}).}

\item{lower_inclusive}{Whether the lower limit itself is inclusive, i.e.
whether we require \code{x >= lower} or \code{x > lower} (the default is the former).}

\item{upper}{The upper limit for \code{x} (default = \code{Inf}).}

\item{upper_inclusive}{Whether the upper limit itself is inclusive, i.e.
whether we require \code{x <= upper} or \code{x < lower} (the default is the former).}

\item{allow_missing}{Whether x is allowed to be missing (default = \code{FALSE}).
If \code{allow_missing} is \code{TRUE} we skip the tests comparing \code{x} to \code{lower} and
\code{upper}.}
}
\value{
\code{invisible(TRUE)}, if no problems were found; otherwise a
\code{stopifnot()} is triggered.
}
\description{
Checks a variable is a single number (and optionally, in a range and not NA)
}
\examples{
# These are all OK:
check_numeric(0)
check_numeric(0L)
check_numeric(0, lower = 0)
check_numeric(0, upper = 0)
check_numeric(NA_real_, allow_missing = TRUE)
check_numeric(Inf)
check_numeric(-Inf)
# These return errors:
try(check_numeric("foo"))
try(check_numeric(1:2))
try(check_numeric(NA_real_))
try(check_numeric(NA, allow_missing = TRUE)) # NA is logical, not numeric
try(check_numeric(0, lower = 1))
try(check_numeric(0, lower = 0, lower_inclusive = FALSE))
}
