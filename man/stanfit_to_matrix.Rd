% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stan_summaries.R
\name{stanfit_to_matrix}
\alias{stanfit_to_matrix}
\alias{stanfit_to_dt}
\title{Converts a stanfit object to a matrix or data.table}
\usage{
stanfit_to_matrix(stanfit, params_desired = NA)

stanfit_to_dt(stanfit, params_desired = NA)
}
\arguments{
\item{stanfit}{A stanfit object e.g. as returned by \code{\link[rstan:stanmodel-method-sampling]{rstan::sampling()}}.}

\item{params_desired}{A character vector with the names of those parameters
in the stanfit object that should be included in the output. The default
value of \code{NA} means all parameters are included except \code{"lp__"} (which is not
actually a parameter: Stan uses it to record the value of the target
probability density).}
}
\value{
A matrix or data.table with one column per parameter and one row per
sample.
}
\description{
Converts a stanfit object to a matrix or data.table
}
\examples{
  eg <- mastiff::stan_example_regression$posterior_samples
  stanfit_to_matrix(eg)
  stanfit_to_dt(eg)
}
