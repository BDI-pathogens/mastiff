# R6.interface.implements

Checks to see whether interface `interfaceName` has been implemented on
object `object`.

## Usage

``` r
R6.interface.implements(object, interfaceName)
```

## Arguments

- object:

  R6 object of class `R6.class`.

- interfaceName:

  Name of an interface to check for `object`.
